# PCIL — pinned dependency lock
# ------------------------------
# Pinned to the verified local environment — the SAME env the .pkl bundles were
# trained in — so the Docker/NUC build is reproducible and the runtime
# scikit-learn matches the bundles. (The container previously pulled sklearn
# 1.9.0 and warned about unpickling a 1.8.0 RandomForest.)
#
# Source of the ranges: requirements.in. To refresh: edit requirements.in,
# reinstall, then `pip freeze` the relevant packages back into this file.
# torch is installed first from the CPU wheel index in the Dockerfile.

# Pipeline #1 / #2
pandas==2.3.2
numpy==2.4.4
scikit-learn==1.8.0
scipy==1.17.1
joblib==1.5.3
PyYAML==6.0.3

# Orchestrator (Week-3)
fastapi==0.136.3
uvicorn[standard]==0.47.0
pydantic==2.13.4
python-multipart==0.0.29

# Cyclical anomaly: 1D CNN autoencoder. The Dockerfile installs torch==2.12.0
# from the CPU wheel index first; ==2.12.0 here is satisfied by the 2.12.0+cpu
# build. On a dev machine this pulls the default torch 2.12.0.
torch==2.12.0

# Pipeline #3 (RAG)
python-docx==1.2.0
google-genai==2.8.0
psycopg[binary]==3.2.13
transformers==4.46.3
FlagEmbedding==1.4.0
rank-bm25==0.2.2

# Frontend (Flask UI)
Flask==3.0.3
requests==2.34.2

# Tests + run scripts
pytest==9.0.3
httpx==0.28.1
python-dotenv==1.2.2
