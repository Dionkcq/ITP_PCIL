# PCIL — pinned Python dependencies
# ----------------------------------
# Install with: pip install -r requirements.txt
#recommend to create venv and activate before installing dependencies:
# python -m venv .venv
# .\.venv\Scripts\Activate.ps1

# Versions are minimum-required (>=) to keep the file working as
# packages move forward. Pin to exact versions before docker build
# when we lock for the NUC.

# Pipeline #1 / #2
pandas>=2.0
numpy>=1.26
scikit-learn>=1.4
pyyaml>=6.0
joblib>=1.3

# Orchestrator (Week-3)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
python-multipart>=0.0.9   # required by FastAPI for UploadFile + Form (file uploads)

# Pipeline #3 (RAG, Robin's work)
python-docx>=1.1
google-generativeai>=0.7

# Frontend (Flask UI)
flask>=3.0
requests>=2.31

# Tests
pytest>=7.4
httpx>=0.27               # used by FastAPI TestClient
