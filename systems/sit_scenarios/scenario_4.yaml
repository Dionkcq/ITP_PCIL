# ============================================================
# SIT ITP Test Scenario 4 (Winardi, 2026-07-06) — GENERATED
# by sit_testing/generate_recipes.py from 'SIT ITP Test Scenarios.xlsx'.
# Expected result (verbatim from the workbook):
#   Machine 1 breaks down, forcing downstream machines 2 and 3 to stop
#   because they run out of parts. High collinearity. Linear regression
#   will split weights erratically without regularization.
# Data: table scenario_4 (restored from itp_test_scenarios.sql).
# ============================================================

system: sit_scenarios

pipeline:
  output_dir: "output"

trigger:
  source_type: postgres
  table: scenario_4
  mode: "all"
  start_time: null
  end_time: null
  last_n: null

input:
  timestamp_column: timestamp

  numerical_features:
    - m1_mechanical_failure_min
    - m2_starved_min
    - m3_starved_min
    - logistics_delay_min

  categorical_features: []

  targets:
    - line_oee_pct

feature_descriptions:
  m1_mechanical_failure_min: "M1 mechanical failure (minutes) over the window."
  m2_starved_min: "M2 starved (minutes) over the window."
  m3_starved_min: "M3 starved (minutes) over the window."
  logistics_delay_min: "Logistics delay (minutes) over the window."
