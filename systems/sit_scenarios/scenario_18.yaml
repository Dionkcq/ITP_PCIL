# ============================================================
# SIT ITP Test Scenario 18 (Winardi, 2026-07-06) — GENERATED
# by sit_testing/generate_recipes.py from 'SIT ITP Test Scenarios.xlsx'.
# Expected result (verbatim from the workbook):
#   Physical breakdowns cause errors, but the PLC heat probe has crashed
#   and reads a flatlined constant. The model outputs a 0 coefficient
#   for the dead feature because it contains zero variance.
# Data: table scenario_18 (restored from itp_test_scenarios.sql).
# ============================================================

system: sit_scenarios

pipeline:
  output_dir: "output"

trigger:
  source_type: postgres
  table: scenario_18
  mode: "all"
  start_time: null
  end_time: null
  last_n: null

input:
  timestamp_column: timestamp

  numerical_features:
    - heater_1_temp_sensor
    - die_pressure_bar
    - line_speed_mps

  categorical_features: []

  targets:
    - extruder_thickness_error_mm

feature_descriptions:
  heater_1_temp_sensor: "Heater 1 temp sensor over the window."
  die_pressure_bar: "Die pressure (bar) over the window."
  line_speed_mps: "Line speed (metres per second) over the window."
